import styled from "styled-components";

export const Option = styled.input({
    width:' 18px',
    height: '18px',
    cursor: 'pointer',
    margin: '0',
    accentColor: '#007bff',
})

export const Label = styled.label({
    fontSize: '1.2rem',
    letterSpacing: '1px',
})